# Risk log (to tackle in S1/S2)

- video1.py uses a process-killing path on Grok fallback → replace with safe Pixabay fallback.
- test_parts_1_2.py should export: render_final_video(script) -> bytes (final MP4).
- Add FastAPI wrapper (service/app.py) with Supabase upload + X-API-Key.
- Container/Dockerfile needs ffmpeg install.
